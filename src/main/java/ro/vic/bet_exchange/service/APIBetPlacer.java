package ro.vic.bet_exchange.service;

import ro.vic.bet_exchange.model.Match;

public class APIBetPlacer implements BetPlacer {
    @Override
    public boolean placeBet(Match match, double stake) {
        // Code to call the betting API, handle authentication, etc.
        // Return true if the bet is placed successfully.
        return true;
    }
}
